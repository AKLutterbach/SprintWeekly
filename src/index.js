// Deprecated server-side UI Kit implementation removed.
// This file is intentionally left minimal to avoid any server-side UI Kit
// patterns (for example, imports from `@forge/ui` or server-render exports).
// The app now uses a client-side frontend resource (src/frontend) plus
// a resolver function (src/resolvers/index.js).

// Keep a harmless export so the module exists but doesn't trigger platform checks.
export default null;
